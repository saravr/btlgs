<%= form_for(@auto_cat) do |f| %>
  <% if @auto_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_cat.errors.count, "error") %> prohibited this auto_cat from being saved:</h2>

      <ul>
      <% @auto_cat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent %><br />
    <%= f.text_field :parent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
