<%= form_for(@home_cat) do |f| %>
  <% if @home_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_cat.errors.count, "error") %> prohibited this home_cat from being saved:</h2>

      <ul>
      <% @home_cat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
