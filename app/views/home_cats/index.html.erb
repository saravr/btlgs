<h1>Listing home_cats</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_cats.each do |home_cat| %>
  <tr>
    <td><%= home_cat.name %></td>
    <td><%= link_to 'Show', home_cat %></td>
    <td><%= link_to 'Edit', edit_home_cat_path(home_cat) %></td>
    <td><%= link_to 'Destroy', home_cat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Home cat', new_home_cat_path %>
